"""Plugin directory for Codex worker adapters.

Each subdirectory is a self-contained worker plugin:
- worker.manifest.json: capability metadata + arg schema
- adapter.py: collect_bundle() / validate_bundle() implementation
"""
